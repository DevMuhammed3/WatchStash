{
  "name": "watchstash",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.1.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.14",
  "private": true,
  "scripts": {
    "postinstall": "mkdir -p node_modules/@watchstash && ln -sf ../../packages/types node_modules/@watchstash/types && ln -sf ../../packages/ui node_modules/@watchstash/ui",
    "dev": "concurrently -n backend,frontend -c blue,green \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "bun --watch --cwd apps/backend src/index.ts",
    "dev:frontend": "bun run --cwd apps/frontend dev"
  },
  "type": "module",
  "workspaces": ["packages/types", "packages/ui", "apps/frontend", "apps/backend"]
}
